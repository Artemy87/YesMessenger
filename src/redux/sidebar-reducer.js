let initialState =  {
    friends: [
        {id: 1, name: 'Oleg'},
        {id: 2, name: 'Dmitriy'},
        {id: 3, name: 'Nikita'},
    ],
};

const sidebarReducer = (state=initialState, action) => {

    return state;
}

export default sidebarReducer;